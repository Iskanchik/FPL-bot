
services:
  - type: web
    name: fpl-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python fpl_bot.py
    healthCheckPath: /health
    autoDeploy: true
