services:
  - type: web
    name: fpl-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python fpl_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0